```python
import matplotlib as mpl
import matplotlib.pyplot as plt
import numpy as np
```

```python
plt.style.use('classic')
```

```python
x = np.linspace(0, 10, 100)
fig = plt.figure()

plt.plot(x, np.sin(x), '-')
plt.plot(x, np.cos(x), '--')

fig.savefig('files/fig1.png')
```

```python
plt.figure()

plt.subplot(2, 1, 1)
plt.plot(x, np.sin(x))

plt.subplot(2, 1, 2)
plt.plot(x, np.cos(x), '--')
```

```python
fig, ax = plt.subplots(2)

ax[0].plot(x, np.sin(x))
ax[1].plot(x, np.cos(x))
```

```python
fig = plt.figure()
ax = plt.axes()

x = np.linspace(0, 10, 1000)
ax.plot(x, np.sin(x))
```

```python
plt.plot(x, np.sin(x - 0), color='blue')
plt.plot(x, np.sin(x - 1), color='g')
plt.plot(x, np.sin(x-2), color='0.75') # Шкала відтінків сірого кольору від 0 до 1
plt.plot(x, np.sin(x-3), color='#FFDD44')
plt.plot(x, np.sin(x-4), color=(1.0, 0.2, 0.3)) # RGB
plt.plot(x, np.sin(x-5), color='chartreuse') # Назви кольорів HTML

plt.plot(x, x+0, linestyle='solid')
plt.plot(x, x+1, linestyle='dashed')
plt.plot(x, x+2, linestyle='dashdot')
plt.plot(x, x+3, linestyle='dotted')
```

```python
plt.plot(x, x+0, linestyle='-')
plt.plot(x, x+1, linestyle='--')
plt.plot(x, x+2, linestyle='-.')
plt.plot(x, x+3, linestyle=':')
```

```python
plt.plot(x, x+0, '-g') # green
plt.plot(x, x+1, '--c') # cyan
plt.plot(x, x+2, '-.k') # blacK
plt.plot(x, x+3, ':r') # red
```

```python
plt.plot(x, np.sin(x))
plt.xlim(-1, 11)
plt.ylim(-1.5, 1.5)
```

```python
plt.plot(x, np.sin(x))
plt.ylim(1.5, -1.5)
plt.xlim(11, -1) # зеркально, якщо аргументи вказати у зворотному порядку
```

```python
plt.plot(x, np.sin(x))
plt.axis([-1, 11, -1.5, 1.5]) # [xmin, xmax, ymin, ymax]
```

```python
plt.plot(x, np.sin(x))
plt.axis('tight') # підлаштовується під розмір екрану
```

```python
plt.plot(x, np.sin(x))
plt.axis('equal') # масштаб x та y в координатних осях однаковий (без нього коло може стати овалом, наприклад, бо matplotlib розтягує по всьому вікну)
```

```python
plt.plot(x, np.sin(x))

plt.title('A Sine Curve')
plt.xlabel('x')
plt.ylabel('sin(x)')
```

```python
plt.plot(x, np.sin(x), '-g', label='sin(x)')
plt.plot(x, np.cos(x), ':b', label='cos(x)')

plt.axis('equal')
plt.legend()
```

```python
# plt.xlabel() -> ax.set_xlabel()
# plt.ylabel() -> ax.set_ylabel()
# plt.xlim() -> ax.set_xlim()
# plt.ylim() -> ax.set_ylim()
# plt.title() -> ax.set_title()
```

```python
plt.style.use('seaborn-v0_8-whitegrid')

x = np.linspace(0, 10, 30)
y = np.sin(x)

plt.plot(x, y, 'o', color='black')
```

```python
plt.plot(x, y, '-ok') # solid, o - marker, k - blacK
```

```python
plt.plot(x, y, '-p', color='gray', markersize=15, linewidth=4,
         markerfacecolor='white',
         markeredgecolor='gray',
         markeredgewidth=2)

plt.ylim(-1.2, 1.2)
```

```python
rng = np.random.RandomState(0)

x = rng.randn(100)
y = rng.randn(100)

colors = rng.rand(100)
sizes = 1000 * rng.rand(100)

plt.scatter(x, y, c=colors, s=sizes, alpha=0.3, cmap='viridis')

plt.colorbar()
```

```python
from sklearn.datasets import load_iris

iris = load_iris()

features = iris.data.T

plt.scatter(features[0], features[1], alpha=0.2, s=100*features[3], c=iris.target, cmap='viridis')

plt.xlabel(iris.feature_names[0])
plt.ylabel(iris.feature_names[1])
```

```python
x = np.linspace(0, 10, 50)

dy = 0.8

y = np.sin(x) + dy * np.random.randn(50)

plt.errorbar(x, y, yerr=dy, fmt='.k') # планки погрішності, fmt значить, що точки будуть маленькими маркерами чорного кольору
```

```python
plt.errorbar(x, y, yerr=dy, fmt='o', color='black', ecolor='lightgray', elinewidth=3, capsize=0)
```

```python
from sklearn.gaussian_process import GaussianProcessRegressor
from sklearn.gaussian_process.kernels import Matern

model = lambda x: x * np.sin(x)

xdata = np.array([1, 3, 5, 6, 8])
ydata = model(xdata)

  
kernel = Matern(length_scale=1.0, length_scale_bounds=(1e-2, 10.0), nu=2.5)
gp = GaussianProcessRegressor(kernel=kernel, n_restarts_optimizer=100)

gp.fit(xdata[:, np.newaxis], ydata)


xfit = np.linspace(0, 10, 1000)
yfit, sigma = gp.predict(xfit[:, np.newaxis], return_std=True)

dyfit = 2 * sigma

plt.plot(xdata, ydata, 'or')
plt.plot(xfit, yfit, '-', color='gray')

plt.fill_between(xfit, yfit - dyfit, yfit + dyfit, color='gray', alpha=0.2)

plt.xlim(0, 10)
```