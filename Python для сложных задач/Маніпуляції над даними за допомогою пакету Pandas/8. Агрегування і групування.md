```python
import numpy as np
import pandas as pd
import seaborn as sns
```
.
```python
planets = sns.load_dataset('planets')
planets.shape
```

```python
planets.head()
```

```python
rng = np.random.RandomState(42)

ser = pd.Series(rng.rand(5))
print(ser.sum(), ser.mean())

df = pd.DataFrame({'A': rng.rand(5),
                   'B': rng.rand(5)})

print(df.mean())
print(df.mean(axis='columns'))
```

```python
planets.dropna().describe()
```

```python
print(planets.count())
print(planets.mean(numeric_only=True))
print(planets.median(numeric_only=True))
print(planets.max(numeric_only=True))
print(planets.min(numeric_only=True))
print(planets.var(numeric_only=True))
print(planets.std(numeric_only=True))
print(planets.prod(numeric_only=True))
print(planets.sum(numeric_only=True))
```

```python
df = pd.DataFrame({'key': ['A', 'B', 'C', 'A', 'B', 'C'],
                   'data': range(6)}, columns=['key', 'data'])

df
```

```python
df.groupby('key') # split-apply-combine
print(df.groupby('key').sum())
```

```python
planets.groupby('method')['orbital_period'].median()
```

```python
for (method, group) in planets.groupby('method'):
    print(method, group.shape)
```

```python
planets.groupby('method')['year'].describe().unstack()
```

```python
rng = np.random.RandomState(42)

df = pd.DataFrame({'key': ['A', 'B', 'C', 'A', 'B', 'C'],
                   'data1': range(6),
                   'data2': rng.randint(0, 10, 6)},
                   columns=['key', 'data1', 'data2'])

df.groupby('key').aggregate(['min', np.median, max])
```

```python
df.groupby('key').aggregate({'data1': 'min',
                             'data2': 'max'})
```

```python
def filter_func(x): # x - це група
    return x['data2'].std() > 2

print(df.groupby('key').std())
df.groupby('key').filter(filter_func)
```

```python
df.groupby('key').transform(lambda x: x - x.mean())
```

```python
def norm_by_data2(x):
    x['data1'] /= x['data2'].sum()
    return x

print(df)
print(df.groupby('key').apply(norm_by_data2))
```

```python
L = [0, 1, 0, 1, 2, 0]

df.groupby(L).sum() # 1, 3 та останній елементи відносяться до однієї групи. key також сумується
```

```python
df.groupby('key').sum()
```

```python
df2 = df.set_index('key')
mapping = {'A': 'vowel', 'B': 'consonant', 'C': 'consonant'} # в поверненому датафреймі замість 'A' буде 'vowel'
df2.groupby(mapping).sum()
```

```python
df2.groupby(str.lower).mean()
```

```python
df2.groupby([str.lower, mapping]).mean() # зразу декілька
```

```python
decade = 10 * (planets['year'] // 10)
decade = decade.astype(str) + 's'
decade.name = 'decade'
planets.groupby(['method', decade])['number'].sum().unstack().fillna(0) # групуємо по кожній комбінації 'method' і decade.
```