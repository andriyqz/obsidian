
```python
import pandas as pd
import numpy as np
```

```python
names = pd.Series(['PETER', 'pAul', 'guido'])
names.str.capitalize()
```

```python
names.str.slice(0, 3)
```

```python
df = pd.DataFrame({'name': names.str.capitalize(),
                   'surname': ['1', '2', '3']})

df['name'].str.cat(df['surname'], sep=' ') # concatenate
```

```python
full_info_df = pd.DataFrame({'name': names.str.capitalize(),
                             'info': ['B|C|D', 'B|D', 'B|C']})
                             
full_info_df['info'].str.get_dummies(sep='|')
```