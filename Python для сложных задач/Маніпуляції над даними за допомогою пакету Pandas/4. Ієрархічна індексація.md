У об'єкті Series можна зберігати навіть двовимірні дані:
```python
index = [('California', 2000), ('California', 2010),
         ('New York', 2000), ('New York', 2010),
         ('Texas', 2000), ('Texas', 2010)]

populations = [1000, 1200,
               1300, 1500,
               1400, 1600]

index = pd.MultiIndex.from_tuples(index)
pop = pd.Series(data=populations, index=index)

```
```python
pop = pop.reindex(index)

pop[:, 2010]
```

```python
pop_df = pop.unstack()
pop_df
```
```
		    2000 2010 
California 1000 1200 
New York 1300 1500 
Texas 1400 1600
```

```python
df = pd.DataFrame(np.random.rand(4, 2),
                  index=[['a', 'a', 'b', 'b'], [1, 2, 1, 2]], # (a, 1); (a, 2); (b, 1); (b, 2)
                  columns=['data1', 'data2'])

df
```
![[Pasted image 20260501003857.png]]

