```python
import pandas as pd
import numpy as np
import seaborn as sns
```

```python
titanic = sns.load_dataset('titanic')
titanic.head()
```

```python
titanic.groupby('sex')[['survived']].mean()
```

```python
titanic.groupby(['sex', 'class'])['survived'].aggregate('mean').unstack()
```

```python
titanic.pivot_table('survived', index='sex', columns='class')
```

```python
age = pd.cut(titanic['age'], [0, 18, 80])
titanic.pivot_table('survived', ['sex', age], 'class')
```

```python
fare = pd.qcut(titanic['fare'], 2) # quantile cut
titanic.pivot_table('survived', ['sex', age], [fare, 'class'])
```

```python
titanic.pivot_table('survived', ['sex', age], aggfunc=np.mean, margins=True)
```

```
!curl -O https://raw.githubusercontent.com/jakevdp/data-CDCbirths/refs/heads/master/births.csv
```

```python
births = pd.read_csv('births.csv')
births.head()
```

```python
births['decade'] = 10 * (births['year'] // 10)
births.pivot_table('births', index='decade', columns='gender', aggfunc='sum')
```

```python
import matplotlib.pyplot as plt

sns.set()

births.pivot_table('births', index='decade', columns='gender', aggfunc='sum').plot()

plt.ylabel('total births per year')
```

```python
quartiles = np.percentile(births['births'], [25, 50, 75])
mu = quartiles[1]
sig = 0.74 * (quartiles[2] - quartiles[0])
```

```python

```