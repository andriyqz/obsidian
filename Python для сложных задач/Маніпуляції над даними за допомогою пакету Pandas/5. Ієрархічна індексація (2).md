```python
```python
index = pd.MultiIndex.from_tuples([  
			('California', 2000),  
			('California', 2010),  
			('Texas', 2000),  
			('Texas', 2010)  
			], names=['state', 'year'])  
  
pop = pd.Series([100, 150, 200, 250], index=index, name='population')
```

```python
pop.loc['California':'Texas']`
```

```python
pop[pop >= 150]
```

```python
health_data['Guido']['HR']
```

```python
health_data.iloc[:2, :2]  
health_data.loc[:, ('Bob', 'HR')]
```

```python
idx = pd.IndexSlice  
health_data.loc[idx[:, 1], idx[:, 'HR']]
```

```python
index = pd.MultiIndex.from_product([['a', 'c', 'b'], [1, 2]])  
data = pd.Series(np.random.rand(6), index=index)  
  
data['a':'b'] # помилка
```

```python
data = data.sort_index()  
data['a':'b'] # працює
```

```python
pop.unstack(level=0)  
pop.unstack(level=1)
print(pop.unstack().stack())
```

```python
pop_flat = pop.reset_index()
pop_flat
```

```python
pop_flat.set_index(['state', 'year'])
```

```python
data_mean = health_data.groupby(level='year').mean()
data_mean
```

```python
data_mean = health_data.T.groupby(level='type').mean() # by columns
data_mean
```