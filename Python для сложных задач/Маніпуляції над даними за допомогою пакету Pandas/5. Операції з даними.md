```python
rng = np.random.RandomState(42)
ser = pd.Series(rng.randint(0, 10, 4))

df = pd.DataFrame(rng.randint(0, 10, (3, 4)), columns=list('ABCD'))

print(np.exp(ser)) # e^ser_value
print(np.sin(df * np.pi/4)) # застосовується до всього датафрейму

area = pd.Series({'Alaska': 1000, 'Texas': 2000, 'New York': 3000, 'California': 2500}, name='area')
population = pd.Series({'Alaska': 10000, 'Texas': 20000, 'New York': 30000}, name='area')

print(population / area)
```

```python
A = pd.Series([2, 4, 6], index=[0, 1, 2])
B = pd.Series([1, 3, 5], index=[1, 2, 3])

  

print(A + B)
print(A.add(B, fill_value=0)) # відсутнім індексам присвоюється це значення перед обрахунком
```

```python
A = pd.DataFrame(rng.randint(0, 20, (2, 2)), columns=list('AB'))
B = pd.DataFrame(rng.randint(0, 10, (3, 3)), columns=list('BAC'))

print(A)
print(B)
print(A + B) # B0 in new array = A_B0 + B_B0
```

```python
fill = A.stack().mean() # для того щоб взяти середнє ВСІЄЇ вибірки, а не по колонках

A.add(B, fill_value=fill)
```

#### + - add()

### - sub(), subtract()

# * - mul(), multiply()

# / - truediv(), div(), divide()

# // - floordiv()

# % - mod()

# ** - pow()