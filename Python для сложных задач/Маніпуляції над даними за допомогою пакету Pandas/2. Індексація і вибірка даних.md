### Вибірка даних із об'єкту Series:
```python
data = pd.Series([0.25, 0.5, 0.75, 1.0],
				  index=['a', 'b', 'c', 'd'])
data
```

```python
data = pd.Series([0.25, 0.5, 0.75, 1.0],
                  index=['a', 'b', 'c', 'd'])

print(data['b'])
print('a' in data)
print(data.keys()) # Index(['a', 'b', 'c', 'd'], dtype='object')
print(list(data.items())) # [('a', 0.25), ('b', 0.5), ('c', 0.75), ('d', 1.0)]

data['e'] = 1.25
data
```

### Об'єкт Series як одновимірний масив:
```python

```