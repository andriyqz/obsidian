Рекурентні нейронні мережі (RNN) — це клас мереж, здатних прогнозувати майбутнє, аналізуючи часові ряди (наприклад, кількість активних користувачів, температуру або траєкторії автомобілів). Окрім RNN, для обробки послідовностей можна використовувати звичайні повнозв'язні мережі (для коротких послідовностей) або згорткові мережі (для дуже довгих, як-от аудіо).

**1. Рекурентні нейрони та шари**
На відміну від мереж прямого поширення, RNN мають зв’язки, що вказують назад.
- **Рекурентний нейрон:** на кожному часовому кроці t (фреймі) він отримує вхідні дані $x_t$​ та свій власний вихід з попереднього кроку $y_{(t-1)}$
- **Розгортання в часі:** представлення нейрона у вигляді ланцюжка, де для кожного кроку відображено його стан.
- **Ваги:** кожен нейрон має два набори ваг — $W_x$​$ для поточних входів та $W_y$​​ для виходів попереднього кроку.

**Математичне обчислення виходу (для міні-батчу):** $Y_{(t)}​=\phi(X_{(t)}​W_x​+Y_{(t−1)​}W_y​​+b)$

**2. Комірки пам'яті та типи послідовностей**
Частина мережі, що зберігає стан між кроками, називається **коміркою пам'яті**.
- **Sequence-to-sequence (послідовність-у-послідовність):** на вхід подається послідовність, на виході — теж (наприклад, температура за кожну з останніх 24 годин $\rightarrow$ Вихід — прогноз температури на кожну з наступних 24 годин.).
- **Sequence-to-vector (послідовність-у-вектор):** ігноруються всі виходи, крім останнього (наприклад, відгук на фільм із 100 слів $\rightarrow$ Вихід — оцінка від 1 до 5 зірок або вектор класифікації («позитивний/негативний»).
- **Vector-to-sequence (вектор-у-послідовність):** один вхід подається повторно на кожному кроці (наприклад, зображення з CNN $\rightarrow$ Вихід — текстовий опис: «Собака біжить по траві).
- **Encoder-Decoder:** послідовність-у-вектор (енкодер), за якою йде вектор-у-послідовність (декодер). Використовується для машинного перекладу

**3. Навчання RNN (BPTT)**
Для навчання використовується алгоритм **зворотного поширення помилки крізь час (Backpropagation Through Time, BPTT)**. Мережа розгортається, обчислюється функція втрат, а градієнти проходять назад крізь усі кроки часу.

**4. Прогнозування часових рядів (Проєкт)**
У главі розглядається задача прогнозування пасажиропотоку в Чикаго.
- **Базова лінія (Naive forecasting):** припущення, що значення буде таким самим, як 7 днів тому.
- **Підготовка даних:** створення вікон (windows) фіксованої довжини, де цільове значення — це наступний крок часу.

**Приклад коду: Реалізація RNN «вручну» (без готових комірок)**:

```python
import torch
from torch import nn

class SimpleRnnModel(nn.Module):
    def __init__(self, input_size, hidden_size, output_size):
        super().__init__()
        self.hidden_size = hidden_size
        # Пам'яттєва комірка: лінійний шар + tanh для стабільності
        self.memory_cell = nn.Sequential(
            nn.Linear(input_size + hidden_size, hidden_size),
            nn.Tanh()
        )
        self.output = nn.Linear(hidden_size, output_size)

    def forward(self, X):
        batch_size, window_length, dimensionality = X.shape
        X_time_first = X.transpose(0, 1) # [час, батч, ознаки]
        H = torch.zeros(batch_size, self.hidden_size, device=X.device)
        
        for X_t in X_time_first:
            # Об'єднуємо вхід та попередній стан
            XH = torch.cat((X_t, H), dim=1) 
            H = self.memory_cell(XH)
            
        return self.output(H)
```

**5. Використання nn.RNN у PyTorch**
PyTorch надає готовий модуль `nn.RNN`, який працює значно швидше. Він повертає тензор усіх станів (`outputs`) та фінальні приховані стани кожного шару (`_hidden`).

**Глибокі RNN:** Можна стакувати рекурентні шари один на одного. Також ефективно навчати модель за схемою **seq2seq**, де на кожному кроці передбачається наступне значення (це дає більше градієнтів і прискорює навчання)

**6. Проблеми та їх вирішення**
- **Нестабільні градієнти:** Вирішується за допомогою **рекурентної Layer Normalization** (додається в цикл вручну) або **Dropout** (параметр `dropout` у `nn.RNN` діє між шарами).
- **Обмежена коротка пам'ять:** Прості RNN втрачають інформацію вже через 10 кроків («проблема рибки Дорі»)

**7. LSTM та GRU комірки**
- **LSTM (Long Short-Term Memory, 1997):** Розділяє стан на короткостроковий h(t)​ та довгостроковий c(t)​. Має три гейти: **forget gate** (що стерти), **input gate** (що додати) та **output gate** (що видати на вихід).
- **GRU (Gated Recurrent Unit, 2014):** Спрощена версія LSTM. Об'єднує стани в один h(t)​, використовує один контролер для забування/входу та додає **reset gate** r(t)​ для керування доступом до попереднього стану

![[Pasted image 20260727234611.png]]

**9. Обробка послідовностей за допомогою 1D-згорток**
1D згорткові шари (`nn.Conv1d`) ковзають по послідовності, виявляючи локальні патерни. Використання кроку (stride) більше 1 дозволяє скоротити довжину послідовності, допомагаючи рекурентним шарам бачити довші залежності

**10. Архітектура WaveNet**
Ця архітектура використовує стек **дилатованих (розширених)** згорткових шарів. Дилатація дозволяє охоплювати величезні рецептивні поля (тисячі кроків) з мінімальною кількістю параметрів. Використовується **каузальне заповнення (causal padding)**, щоб модель не бачила майбутнього

```python
class WavenetModel(nn.Module):
    def __init__(self, input_size, hidden_size, output_size):
        super().__init__()
        layers = []
        # Стек шарів з різною дилатацією: 1, 2, 4, 8...
        for dilation in (1, 2, 4, 8) * 2:
            layers += [nn.Conv1d(input_size, hidden_size, kernel_size=2, 
                                 padding=dilation, dilation=dilation),
                       nn.ReLU()]
            input_size = hidden_size
        self.convs = nn.Sequential(*layers)
        self.output = nn.Linear(hidden_size, output_size)

    def forward(self, X):
        Z = X.permute(0, 2, 1) # Час як просторовий вимір
        Z = self.convs(Z)
        # Обрізання зайвого заповнення справа для каузальності
        Z = Z[..., :-self.total_padding] 
        return self.output(Z.permute(0, 2, 1))
```

**11. Класичні статистичні моделі (Сімейство ARMA)**
Ці моделі базуються на припущенні, що часовий ряд є **стаціонарним** (його статистичні властивості не змінюються з часом). Якщо ряд має тренд або сезонність, його потрібно спочатку зробити стаціонарним, наприклад, за допомогою **диференціювання (differencing)**

**ARMA (Autoregressive Moving Average)**
Розроблена Германом Волдом у 1930-х роках. Прогноз будується як сума двох компонентів:
- **Авторегресія (AR):** Зважена сума минулих p значень ряду. Гіперпараметр p визначає глибину «погляду» в минуле.
- **Ковзне середнє (MA):** Зважена сума минулих q помилок прогнозу. Це допомагає коригувати модель на основі нещодавніх відхилень.